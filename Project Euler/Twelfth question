let a = 1;
let p = 1;
while(true) {
    let c = [];
    for (b = a; b > 0; b--) {
        if(a % b === 0) {
            c.push(b);
        }
    }
        p++;
    
    if (c.length > 500) {
        console.log(a);
        break;
    }
    a += p;
}
